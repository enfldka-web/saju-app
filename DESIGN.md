# Design

<!-- impeccable:design-doc -->

## World

먹색(ink) 배경 + 한지(paper) 전경 + 앤틱 골드 포인트. "점집"이 아니라 정돈된 프리미엄 웰니스 앱을 지향한다. 흔한 보라색 그라데이션 타로 앱 톤은 의도적으로 피한다. 이 방향은 사용자가 프로젝트 기획 단계에서 직접 고정한 브리프(다크모드+골드+먹색+세리프헤드라인+오행톤다운, 원색 금지)이며, 이번 빌드는 콘셉트 탐색(concept-seed) 단계를 생략하고 고정 브리프를 그대로 커밋했다.

## Color

Restrained 전략: 중립(먹색/한지) + 단일 액센트(골드). 오행 5색은 데이터를 나타내는 별도의 의미 체계로, UI 액센트와 분리해서만 사용한다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `ink-950` | `#0d0c10` | 기본 배경 |
| `ink-900` | `#131218` | 카드/입력 배경 |
| `ink-800` | `#1b1a22` | 서브틀 배경 (오행 바 트랙 등) |
| `ink-700` | `#26242e` | 보더/디바이더 |
| `ink-600` | `#34313d` | 인풋 보더 |
| `paper-100` | `#f3ede1` | 본문 텍스트 |
| `paper-500` | `#a89d8a` | 보조 텍스트 (대비 6.9~7.3:1) |
| `gold-400/500/600` | `#d8b45f` / `#c19a42` / `#96762e` | CTA, 액센트, 포인트 (대비 7.0~7.4:1) |
| `ohaeng-mok` | `#7c9473` | 목(木) — 소나무 잎 |
| `ohaeng-hwa` | `#b56a4d` | 화(火) — 아궁이 불빛 |
| `ohaeng-to` | `#b08a4e` | 토(土) — 황토 |
| `ohaeng-geum` | `#c9c0a4` | 금(金) — 백동 |
| `ohaeng-su` | `#47617e` | 수(水) — 야간 남색 |

다크모드 전용 (Phase 1 범위, 라이트 모드 미구현 — `docs/SAJU_VALIDATION.md` 밖의 별도 결정 필요 항목).

## Typography

- 헤드라인(`font-display`): Noto Serif KR (500/700/900) — 먹글씨 세리프 인상.
- 본문/UI(`font-sans`): Pretendard Variable (자체 호스팅, `app/fonts/`) — 한글 가독성이 검증된 프리미엄 한국어 UI 서체.
- 원국판 간지 글자는 `font-display`로 강조해 "표(表)"의 격식을 살린다.

## Components

- `components/ui/segmented.tsx`: 2지선다 토글 (양력/음력, 성별). 선택 상태는 골드 채움.
- `components/ui/select-field.tsx`: 커스텀 스타일 select, 화살표 아이콘 직접 드로잉.
- `components/ui/icons.tsx`: 1.5px 스트로크로 통일한 최소 아이콘 세트. 이모지/유니코드 기호 대체 금지.
- `components/result/pillar-table.tsx`: 4주(년/월/일/시) 그리드, 일주 컬럼은 골드 틴트로 은은하게 강조. 시간 미상 시 "시간 미상" 상태 표시.
- `components/result/ohaeng-bar.tsx`: 오행 비율을 하나의 stacked proportion bar + 범례로 표현 (스파크라인/프로그레스 링 장식 아님 — 실제 데이터 시각화).
- `components/result/locked-categories.tsx`: 잠긴 카테고리 리스트 + 블러 처리된 티저 문구 + CTA. 결제 미연동 상태에서는 정직한 "준비 중" 마이크로 상태로 응답.

## Layout

- 모바일 퍼스트: 입력 폼은 `h-dvh` 한 화면(스크롤 없음), CTA는 하단 고정.
- 데스크톱(2차 대응): `md:` 브레이크포인트에서 모바일 폭(`max-w-sm`) 카드를 화면 중앙에 배치하는 것으로 축소 대응 — 별도 데스크톱 전용 레이아웃은 만들지 않음.

## Principles

1. 원색 금지 — 모든 색은 톤다운된 버전만 사용.
2. 카드 남발 금지 — 잠긴 카테고리 리스트는 divide 구분선을 쓴 리스트이지 아이콘+제목+텍스트 카드의 반복이 아니다.
3. 이모지/유니코드 아이콘 대체 금지 — 모든 아이콘은 직접 그린 SVG.
4. 계산 결과(원국/오행)와 해석 텍스트(한줄총평)는 시각적으로도 분리해서 배치 — 계산은 표, 해석은 산문.

## Open Items

- 브랜드명 "간지(干支)"는 제안 단계 — 사용자 확정 필요.
- 라이트 모드 미구현.
- 결과 이미지 공유 카드(OG 이미지)는 별도 작업(Phase 1 Task #9).
