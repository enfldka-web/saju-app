# Design

<!-- impeccable:design-doc -->

## World

**v2 (현재)**: 인장·증서(印章·證書)형. 사주는 "점괘"가 아니라 나에게 발급된 공식 증서라는 컨셉 — 마호가니 원장(元帳) 배경 위에 양피지 증서 카드를 올리고, 주홍 인장(印章) 도장을 시그니처 그래픽으로 쓴다. "점집"도, 흔한 보라색 그라데이션 타로 앱도 아닌, 실제 증서/공문서를 발급받는 느낌을 지향한다.

v1(먹색+골드, 브리프 고정)은 사용자 피드백("결과가 엉성하다")에 따라 폐기되고, 천문도/인장증서/달항아리 3개 콘셉트를 스크린샷으로 비교한 뒤 사용자가 인장·증서형을 직접 선택해 교체됐다. 이번 교체는 콘셉트 시드 스크립트 대신 실제 스크린샷 3종 비교로 진행했다(사용자에게 고지 완료).

## Color

Full palette 전략: 마호가니(배경) + 양피지(증서 카드, 반전된 밝은 표면) + 놋쇠(brass, 포인트) + 주홍 인장(단일 시그니처 컬러, 도장 그래픽에만 사용). 오행 5색은 데이터를 나타내는 별도의 의미 체계로 분리 유지.

| 토큰 | 값 | 용도 |
|---|---|---|
| `ink-950` | `#17130f` | 기본 배경 (마호가니 원장) |
| `ink-900` | `#1c1611` | 카드/입력/섹션 배경 |
| `ink-800` | `#241d15` | 서브틀 배경 |
| `ink-700` | `#2f2619` | 보더/디바이더 |
| `ink-600` | `#3a2f20` | 인풋 보더 |
| `paper-100` | `#ece3d2` | 다크 배경 위 본문 텍스트 (아이보리) |
| `paper-300` | `#d9cba8` | 다크 배경 위 3단계 텍스트 |
| `paper-500` | `#a08e6f` | 다크 배경 위 보조 텍스트 |
| `gold-400/500/600` | `#c9a06e` / `#b5895a` / `#8f6a42` | 놋쇠(brass) — CTA, 킥커, 포인트 |
| `parchment-100` | `#f4ecda` | 증서 카드 표면 (반전 배경) |
| `parchment-ink` | `#2c2418` | 증서 카드 위 본문 텍스트 |
| `parchment-ink-soft` | `#8a7a5c` | 증서 카드 위 보조 텍스트 |
| `parchment-rule` | `#d9cba8` | 증서 카드 내부 디바이더 / 다크 배경 위 라벨 텍스트로도 사용 |
| `seal` / `seal-dark` | `#a7402f` / `#7c2e21` | 주홍 인장 — 원국 카드의 도장 그래픽 **한 곳에만** 사용, 남용 금지 |
| `ohaeng-mok` | `#7d9271` | 목(木) — 소나무 잎 |
| `ohaeng-hwa` | `#b56a4d` | 화(火) — 아궁이 불빛 |
| `ohaeng-to` | `#b08a4e` | 토(土) — 황토 |
| `ohaeng-geum` | `#d8cdae` | 금(金) — 백동 |
| `ohaeng-su` | `#4d6a86` | 수(水) — 야간 남색 |

다크모드 전용 (Phase 1 범위, 라이트 모드 미구현).

## Typography

- 헤드라인(`font-display`): Noto Serif KR (500/700/900) — 증서 제목/인장 각자(刻字) 인상.
- 본문/UI(`font-sans`): Pretendard Variable (자체 호스팅, `app/fonts/`).
- 원국 증서의 "四柱命式" 타이틀은 자간을 넓게(tracking) 줘서 실제 문서 표제처럼.

## Components

- `components/result/pillar-table.tsx`: **핵심 시그니처 컴포넌트.** 양피지(parchment) 카드 위에 4주(년/월/일/시) 표, 안쪽 인셋 보더로 증서 프레임 연출, 우하단에 회전된 주홍 인장(일주 간지 + "日主") 오버레이.
- `components/result/ohaeng-bar.tsx`: 비율 바 + 5칸 ledger 타일(원소 색 hairline + 한자 + 개수).
- `components/result/year-fortune.tsx`: 무료로 제공되는 올해 세운(歲運) 총평 — AI 호출 없이 `lib/saju/sewoon.ts`의 결정론적 오행 생극관계 규칙으로 생성.
- `components/result/category-meta.ts`: 4개 카테고리(커리어=수/재물=토/연애=화/건강=목)에 서로 다른 오행 색을 매핑 — 잠금 리스트의 작은 점, 해제 후 카드의 라벨·헤드라인 색으로 재사용해 카테고리를 시각적으로 구분.
- `components/result/reading-unlock.tsx` 해제 화면: 카테고리별 헤드라인은 `text-xl` 세리프로 크고 쉬운 말로, 하단에 1~12월 월별 운세를 3열 그리드로 표시(`lib/saju/sewoon.ts`의 `getMonthPillarsForYear`로 계산한 실제 월주를 AI 프롬프트에 주입해 근거로 삼음).
- `components/result/reading-unlock.tsx`: 잠금 상태는 리스트, 해제 후에는 카테고리별 독립 카드로 전환 — 각 카드 안에 "지금 이 시기 / 실전 조언 / 주목할 시기" 3단 섹션.
- `components/ui/segmented.tsx`, `select-field.tsx`, `icons.tsx`: 토큰 기반이라 v1→v2 전환 시 별도 수정 없이 색만 자동 반영됨.

## Layout

- 모바일 퍼스트: 입력 폼은 `h-dvh` 한 화면(스크롤 없음), CTA는 하단 고정.
- 데스크톱(2차 대응): `md:` 브레이크포인트에서 모바일 폭(`max-w-sm`) 카드를 화면 중앙에 배치.

## Principles

1. 원색 금지 — 모든 색은 톤다운된 버전만 사용.
2. **주홍 인장은 도장 그래픽 한 곳에만** — 버튼/텍스트 액센트로 남용하면 "시그니처 모먼트"가 희석된다. 인터랙션 액센트는 놋쇠(brass, gold-*)로.
3. 카드 남발 금지 — 잠긴 카테고리 리스트는 divide 구분선을 쓴 리스트, 해제 후에도 아이콘+제목+텍스트의 반복 카드가 아니라 실제 콘텐츠 밀도가 다른 3단 구조.
4. 이모지/유니코드 아이콘 대체 금지 — 모든 아이콘은 직접 그린 SVG.
5. 계산 결과(원국 증서/오행)와 해석 텍스트(AI 상세 리딩)는 시각적으로도 분리 — 계산은 증서/표, 해석은 산문.
6. **예외**: `components/auth/login-button.tsx`의 카카오 로그인 버튼은 원색 금지 원칙의 유일한 예외 — 카카오 브랜드 가이드라인이 정한 `#FEE500` 고정 색상을 그대로 사용해야 한다(써드파티 로그인 버튼 규정).

## Open Items

- 브랜드명 "간지(干支)"는 제안 단계 — 사용자 확정 필요.
- 라이트 모드 미구현.
- 공유 카드(`app/api/share-card/route.tsx`, next/og ImageResponse, 1200x630): 완료. 증서 카드 + 올해 총운 + 요청 host를 워터마크로 삽입. `/result` 페이지 `generateMetadata`로 og:image/twitter:image에 연결해 카카오톡 등에 링크 공유 시 자동으로 미리보기가 뜬다. `components/result/share-button.tsx`가 Web Share API(모바일)와 클립보드 복사(데스크톱)를 함께 처리.
